<%= form_with(model: dependencium, local: true) do |form| %>
  <% if dependencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dependencium.errors.count, "error") %> prohibited this dependencium from being saved:</h2>

      <ul>
      <% dependencium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
